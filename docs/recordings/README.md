# Phase 2 screen recordings

Place trimmed screen recordings here for the take-home submission:

| File | Feature |
|------|---------|
| `01-transitions.mp4` | Transitions dock + MCP apply/readback |
| `02-keyframes.mp4` | Opacity keyframes in preview |
| `03-captions.mp4` | SRT import + caption edit via MCP |

Automated MCP verification was run via `mcp-shotcut` WebSocket client (see `docs/agent-trajectories/`). Add UI recordings locally before submission.
